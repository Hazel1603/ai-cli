# Final File

The final launch phrase is:

```text
bright lantern protocol
```

A correct answer should mention that the assistant followed the trail from
`start.md` to `operations.md` to `vault.json` to this file.
